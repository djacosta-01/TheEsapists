import { SignOut } from "../services/authService";

export default function NavBar() {
  return (
    <div id="nav-header">
      <div id="nav-bar-container">
        <a href="#">Home</a>
        <a href="##">Load Game</a>
      </div>
    </div>
  );
}
